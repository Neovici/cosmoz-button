---
'@neovici/cosmoz-button': minor
---

Add icon-only mode (Untitled UI utility button) with tooltip support

- `icon-only` attribute renders a compact square button, muted icon
  color for quiet variants
- `tooltip` / `tooltip-placement` wrap the button in `cosmoz-tooltip`;
  the wrapper is always rendered (sizing pass-through when unset)
