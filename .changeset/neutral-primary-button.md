---
'@neovici/cosmoz-button': minor
---

Make the default (primary) button a neutral dark gray instead of brand blue

Primary now fills with `gray-800` (`gray-700` in dark mode), lightening one
step on hover and darkening one on press. The selected (`aria-pressed`)
primary state follows. Other variants are unchanged.
