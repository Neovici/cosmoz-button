---
'@neovici/cosmoz-button': minor
---

Add icon-only mode (Untitled UI utility button)

The `icon-only` attribute renders a compact square button (28–40px
across sizes) with a muted icon color for the quiet variants
(secondary/tertiary).
